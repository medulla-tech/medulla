This directory contains every material needed to create the database
required by the PULSE2 module to store its informations.

As the PULSE2 evolve, so does the PULSE2 database. This directory contains the
base schema (schema-001.sql) and subsequents evolutions (schema-xxx.sql).

To obtain a state-of-the-art PULSE2 database, just run the install script.

