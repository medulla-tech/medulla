# Installer le serveur SSH du module LSC et la cle id_dsa.pub
# desc_en: Install the LSC SSH service and the id_dsa.pub key
Mount 1

mkdir -p /mnt/tmp

cp /opt/winutils/setupssh.exe /mnt/tmp
cp /revoinfo/data/id_dsa.pub /mnt

echo "cd c:\tmp
setupssh.exe /S
del c:\tmp\setupssh.exe" | unix2dos > /mnt/tmp/setup.bat

RegistryAddRunOnce 'c:\tmp\setup.bat'
