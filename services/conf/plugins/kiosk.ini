[main]
disable = 0
tempdir=/var/tmp/mmc-kiosk

[database]
dbdriver = mysql
dbhost = localhost
dbport = 3306
dbname = kiosk
dbuser = mmc
dbpasswd = mmc
# dbsslenable = 0
# dbsslca =
# dbsslcert =
# dbsslkey =e

[providing]
# Specify if the kiosk uses external ldap (use_external_ldap=true) find infos from database (use_external_ldap=false)
use_external_ldap=false
