[parameters]
# Interval between two batches of reconfiguration commands
# generate_reconf_interval = 60

# Number of concurrent reconfiguration commands
# concurrentreconf = 240

# Timeout waiting for a reconfiguration
# timeout_reconf = 500
