# -*- coding: utf-8; -*-


def action( objetxmpp, action, sessionid, data, message, ret, dataobj):
    try:
       print "restart bot machine %s"%smessage['from']
    except Exception as e:
        print "ERREUR DANS PLUGINS restart bot%s"%str(e)
