#!/usr/bin/python

import lmc.plugins.base
import sys

if len(sys.argv) == 2:
    # Remove $ in machine name
    machine = sys.argv[1][:-1]
    lmc.plugins.base.addMachine(machine, "", True)

sys.exit(0)
