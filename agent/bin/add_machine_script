#!/usr/bin/python

import lmc.plugins.samba
import sys

if len(sys.argv) == 2:
    # Remove $ in machine name
    machine = sys.argv[1][:-1]
    lmc.plugins.samba.addMachine(machine, "", True)

sys.exit(0)
